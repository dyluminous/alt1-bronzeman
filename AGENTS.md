# Bronzeman — Alt1 RuneScape inventory tracker

Build: `npm run build` — webpack bundles into `dist/main.js`

## Directory structure

```
src/
  index.ts                      Entry — bootstrap + Bronzeman.* re-exports for HTML onclick
  bootstrap.ts                  App initialisation after DOM ready
  core.ts                       Shared state, capture helpers, notifications, log, escHtml
  version.ts                    Build number (auto-generated by prebuild)
  style.css                     (removed — split into styles/)
  styles/
    main.css                    Master import — cascades variables → base → components
    variables.css               :root custom properties (colors, fonts, z-index, shadows)
    base.css                    Reset, body, scrollbar, #log
    components/
      tabs.css                  #tab-header, .tab-bar, .tab-btn, .tab-underline, .tab-panel
      buttons.css               .btn, .btn-primary, .pixel-checkbox, .spinner-*, .icon-btn
      layout.css                .status-bar, .section-title, .settings-*, .dev-*, .anchor-dot, manual-unlock, db-search
      search.css                .text-input, .search-panel, .search-results, .search-field-*
      modal.css                 .modal, .modal-content, .loading-overlay, .spinner
      slots.css                 .slot-debug-*, .wiki-disambig-*, .manual-unlock-*, .recent-unlock-cell
      notifications.css         #notification_container, .notif-*
      tooltip.css               #recent_unlock_tooltip, .recent-unlock-tooltip
      unlocks.css               .pickup-card
  types/
    index.ts                    All shared interfaces / type definitions
    fonts.d.ts                  Alt1 font module declarations
    png.d.ts                    PNG module declaration
  utils/
    colors.ts                   Shared alt1 mixColor constants (RS_GOLD, RS_GREEN, etc.)
    hash.ts                     Interior-hash computation (hashInterior, lowerHalfOf, nibbleTolerantMatch)
    helpers.ts                  Pure shared helpers (readPixel, rgbMatch, rgbNear, hashChecksum, formatElapsed)
    gold-dot-asset.ts           Gold dot overlay asset + draw position
  classes/
    inventory.ts                Inventory class — grid state, slot access, singleton `inventory`
    inventory-slot.ts           InventorySlot class — geometry + per-slot item checks
    slot-loading-animation.ts   SlotLoadingAnimation class — gold comet-ring border animation
  services/
    wiki.ts                     RuneScape wiki API — fetchItemTradeable, pickImageForQuantity
    inventory/
      inventory-detect.ts       Pure grid detection (fingerprints, measureGapToSlot2, countColumns, detectInventoryGrid)
      inventory-capture.ts      Auto-capture lifecycle + retry loop
      inventory-scan.ts         SlotScanner — monitors slot contents for appeared/removed/changed/moved events
      inventory-hover.ts        Polls cursor, draws yellow hover square
      inventory-resize-watch.ts Watches border pixels; fires re-calibration on resize/move
    unlock/
      unlock-store.ts           UnlockStore — IndexedDB + in-memory unlock persistence
      unlock-hover.ts           UnlockHoverFlow — gold-dot refresh + hover-to-unlock wiki pipeline
      unlock-backup.ts          Backup/restore unlock DB to/from gzipped JSON
    overlay/
      overlay-draw.ts           RS overlay drawing (drawDetectDebug, debugFindSlot, drawSlotHover, etc.)
      tooltip-scanner.ts        TooltipScanner + readTooltipItemName — tooltip border scan + OCR
      ge-detection.ts           GE interface detection + debug overlays
  ui/
    ui.ts                       DOM rendering + UI action handlers
    modal.ts                    Confirm dialog
    unlock-recent.ts            Ring buffer of recently unlocked items
    unlock-manual.ts            Manual item unlock flow
```

## Rules

- Don't commit unless the user explicitly asks you to commit.
- New code goes in the file that owns its concern. If no file owns it, create one.
- `index.ts` only imports + bootstraps + re-exports. Never dump logic there.
- HTML buttons use `onclick="Bronzeman.fn(...)"`. Those fns are re-exported by index.ts.
- Dependency direction: `core` ← `data`/`inventory`/`detect` ← `ui`/`overlay`/`modal` ← `capture` ← `index`
- All shared interfaces/types live in `types/index.ts` — import from there, re-export where needed.
- Pure helper functions live in `utils/helpers.ts` (no DOM, no state, no side effects).
- Classes (Inventory, InventorySlot, SlotLoadingAnimation) live in `classes/`.
- Orchestration modules (singletons, timers, polling loops) live in `services/`.
- DOM rendering and UI interaction handlers live in `ui/`.
- Never use `any` — prefer `unknown` with type guards/narrowing.
